#!/bin/sh

echo "Removing symlinks from /usr/bin"

for FILENAME in /usr/lib/${HZ_DISTRIBUTION}/bin/hz*; do
  case "${FILENAME}" in
    *bat)
      ;;
    *)
      rm /usr/bin/"$(basename "${FILENAME}")"
      ;;
  esac
done
