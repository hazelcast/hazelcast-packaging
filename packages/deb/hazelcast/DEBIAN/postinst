#!/bin/sh

for FILENAME in /usr/lib/${HZ_DISTRIBUTION}/bin/hz*; do
  case "${FILENAME}" in
    *bat)
      ;;
    *)
      ln -s "${FILENAME}" /usr/bin/"$(basename "${FILENAME}")"
      ;;
  esac
done

printf "\n\nHazelcast is successfully installed to '/usr/lib/${HZ_DISTRIBUTION}/'\n"
